{
  "name": "nd-iot-edge-gateway",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mqtt": "echo 'systemctl start mqtt-broker'",
    "edge": "echo \"123456\" | git pull && node ./index.js -u ./data",
    "start": "npm run mqtt && npm run edge"
  },
  "author": "CZQ",
  "license": "ISC",
  "dependencies": {
    "node-red": "2.2.2"
  }
}
